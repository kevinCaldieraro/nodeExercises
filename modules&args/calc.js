let calc = {
  sum: (x, y) => {
    return x + y;
  },

  mult: (x, y) => {
    return x * y;
  }
};

module.exports = calc;

/* Outra maneira possível de ser feito ************************

module.exports = {
  sum: (x, y) => {
    return x + y;
  },

  mult: (x, y) => {
    return x * y;
  }
};

**************************************************************/
