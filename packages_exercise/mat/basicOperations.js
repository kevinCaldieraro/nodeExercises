module.exports.sum = function sum(x, y) {
  return x + y;
};

module.exports.less = function less(x, y) {
  return x - y;
};

module.exports.multiply = function multiply(x, y) {
  return x * y;
};

module.exports.divide = function divide(x, y) {
  return x / y;
};
